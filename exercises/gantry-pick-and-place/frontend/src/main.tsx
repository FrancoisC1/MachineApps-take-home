import React from 'react';
import ReactDOM from 'react-dom/client';
import RobotControlPanel from './robot-control';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RobotControlPanel />
  </React.StrictMode>
);
